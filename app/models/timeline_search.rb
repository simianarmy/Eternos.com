# $Id$
#
# Class that implements logic for searching a member's data to populate the Timeline

class TimelineSearch
  def self.search(options)
    @response = TimelineRequestResponse.new
    # do search stuff
    @response
  end
end