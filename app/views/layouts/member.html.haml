!!!
%html{html_attrs('en')}
  %head
    %meta{:'http-equiv'=>"Content-Type", :content=>"text/html; charset=utf-8"}/
    %title= "Eternos Members - " + h(yield(:title) || "Home")
    / bundle do
    - stylesheet 'member', 'screen', 'application'
    - javascript :defaults, 'scriptaculous'
    - use_lowpro unless @no_prototype
    = yield(:head)
    = yield(:javascript)
    
    / end bundle
  %body
    #wrapper
      #header
        #logo.alignleft
          %h1= link_to((image_tag 'eternos.gif', :id => 'eternos_logo', :border => '0'),  root_path)
          /= render :partial => 'shared/login_box' unless @hide_login_box
        #menu.alignright
          - cache 'user_nav' do
            = render :partial => 'users/nav'
        .clearboth
      
      #topdiv
      #content
        .content_title.alignleft
          = yield(:title_icon)
          %h2= yield(:title)
            
        #inner_menu.alignright
          %ul#navlist
            %li.home= link_to 'homepage', member_home_path
            %li.support= link_to_lightview("account setup", account_setting_path, {:class => "account-setting"}, {:title => "'Account Setup'"},{:autosize => false, :width => 900, :height => 700})
            %li.logout= link_to 'log out', logout_path
        .clearboth
        
        / for rjs flash notices
        #notice.flash_notice
        = show_flash_messages
        /= render :partial => 'users/user_bar'
        .context
          %p
            = yield(:header)

        #sub_content
          .content_title.alignleft
            / %h2 Copy here necessary?
          .clearboth
          %p
          %br
            = yield
         
        / end of sub content
        .clearboth
        
      / end of content area  
      #bd
      = render :partial => 'shared/site_footer'
    
    / Start Getsatisfaction widget code here
    :plain
    <style type='text/css'>@import url('http://s3.amazonaws.com/getsatisfaction.com/feedback/feedback.css');</style>
    <script src='http://s3.amazonaws.com/getsatisfaction.com/feedback/feedback.js' type='text/javascript'></script>
    <script type="text/javascript" charset="utf-8">
    var tab_options = {}
    tab_options.placement = "right";  // left, right, bottom, hidden
    tab_options.color = "#222"; // hex (#FF0000) or color (red)
    GSFN.feedback('http://getsatisfaction.com/eternoscom/feedback/topics/new?display=overlay&style=problem', tab_options);
    </script>
    / End Getsatisfaction widget code here