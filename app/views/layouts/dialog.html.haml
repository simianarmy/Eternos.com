!!!
%html{html_attrs('en')}
  %head
    %meta{:'http-equiv'=>"Content-Type", :content=>"text/html; charset=utf-8"}/
    %title= "Eternos Members - " + h(yield(:title) || "Home")
    / bundle do
    - stylesheet 'member', 'screen', 'application'
    - load_prototype unless @no_prototype
    - use_lightview
    - use_lowpro
    - javascript 'application'
    = yield(:head)
    = yield(:js_libs)
    = yield(:javascript)
      
    / end bundle
  %body
    #container
      .content_title.alignleft
        %h2= yield(:title)
        
      / for rjs flash notices
      - unless params[:controller] == "facebook_backup"
        #notice.flash_notice= show_flash_messages
      .context
        .clearboth
        %p= yield(:header)
      = yield
      / end of sub content
      .clearboth