page.call 'unload_busy'
page.flash_and_fade :relationship_notice
unless @relationship.new_record?
  page.insert_html :bottom, :relationships, :partial => 'relationship', 
      :locals => {:relationships => [@relationship]}
end