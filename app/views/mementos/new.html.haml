- stylesheet 'mementos'
- javascript 'mementos/editor'
- use_busy
- use_jquery
- use_scrollable

#artifact_picker
  #type_list
    %ul
      %li= link_to 'Photos icon here', contents_url(:type => 'albums')
      %li= link_to 'Videos icon', contents_url(:type => 'web_videos')
      %li= link_to 'Audio icon', contents_url(:type => 'audio')
      %li= link_to 'Music icon', contents_url(:type => 'music')
  #artifact_view
    Expanded artifacts list here
.clear
#slideshow_droptarget
  Where artifacts are dragged to make slideshow
  
  #scrollable_decorations
    /.navi
    %a.prevPage
    #scroller.scrollable
      #item_list.items
        .decoration_item
          Drag & Drop 1st Artifact Here
    %a.nextPage
    
.clear
#preview_pane
  Preview slideshow here?
  
:javascript
  window._token = '#{form_authenticity_token}';
  var editor, artifactPicker;
  
  document.observe('dom:loaded', function() {
    editor = MementoEditor.init();
  });