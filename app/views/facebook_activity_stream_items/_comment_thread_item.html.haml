.comment_thread_item[comment_thread_item]
  .comment_text
    -# Normalize the comment object
    - comment = ActivityStreamItem.normalize_comment_data(comment_thread_item)
    = image_tag(comment.user_pic, :align=>'left') if comment.user_pic
    == On #{comment.posted_at.to_s(:short)}, 
    - if comment.username.any?
      = link_to comment.username, comment.profile_url, :target => '_new'
    - else
      You
    said:
    %br
    %span.bold= comment.comment
  .clearboth


