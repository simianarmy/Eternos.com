- item = facebook_activity_stream_item
.content
  .title_post Facebook Post
  .image_post
    = image_tag 'fb.png'
  .clearboth
  %p.message Message:
  .message_content
    - if item.thumbnail_url
      = link_to image_tag(item.thumbnail_url, :class => 'pic_message'), item.url
    - elsif item.url
      = link_to item.url, item.url

    %p= item.message
    .clearboth
    - if item.author
      == Author: #{item.author}
      %br
    %p.posted
      == Posted: #{item.published_at}
      - if item.published_at != item.edited_at
        %br== Edited: #{item.edited_at}
      
