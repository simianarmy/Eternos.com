%h1 New timeline_event

= error_messages_for :timeline_event

- form_for(:timeline_event, :url => collection_url) do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Create"

= link_to 'Back', collection_url