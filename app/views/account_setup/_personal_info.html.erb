<div id="account-setup-content">
  <%= render :partial => 'account_settings/personal_info_form', :locals => {:settings => settings, :completed_steps => @completed_steps, :setup => true} %>
</div>
<%= javascript_tag "
  document.observe('dom:loaded', function() { datePickerController.create() }); "
%>
