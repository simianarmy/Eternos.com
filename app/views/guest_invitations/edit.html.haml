%h1 Editing guest_invitation

= error_messages_for :guest_invitation

- form_for(:guest_invitation, :url => object_url, :html => { :method => :put }) do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Update"
  
= link_to 'Show', object_url
|
= link_to 'Back', collection_url